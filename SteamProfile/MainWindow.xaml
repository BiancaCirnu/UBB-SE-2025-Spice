<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SteamProfile.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SteamProfile"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Title="SteamProfile - Users">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock 
            Text="Users List" 
            Style="{StaticResource TitleTextBlockStyle}"
            Margin="0,0,0,20"/>

        <controls:DataGrid 
            x:Name="UsersGrid"
            Grid.Row="1"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            GridLinesVisibility="Horizontal"
            HorizontalGridLinesBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}"
            AlternatingRowBackground="{ThemeResource SystemControlBackgroundListLowBrush}">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                <controls:DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <controls:DataGridTextColumn Header="Developer" Binding="{Binding IsDeveloper}" Width="Auto"/>
                <controls:DataGridTextColumn Header="Created" Binding="{Binding CreatedAt}" Width="Auto"/>
                <controls:DataGridTextColumn Header="Last Login" Binding="{Binding LastLogin}" Width="Auto"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</Window>
