<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SteamProfile.Views.ConfigurationsView.AccountSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SteamProfile.Views.ConfigurationsView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <Grid Margin="30" HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel Width="400" Spacing="15">
            <TextBlock Text="Account Settings" FontSize="30" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="Modify Username" FontSize="18" FontWeight="SemiBold"/>
            <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBox Width="250" PlaceholderText="New username"
                         Text="{x:Bind ViewModel.Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Update" Width="100" Command="{x:Bind ViewModel.UpdateUsernameCommand}"/>
            </StackPanel>

            <TextBlock Text="Modify Email" FontSize="18" FontWeight="SemiBold"/>
            <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBox Width="250" PlaceholderText="New email"
                         Text="{x:Bind ViewModel.Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Update" Width="100" Command="{x:Bind ViewModel.UpdateEmailCommand}"/>
            </StackPanel>

            <TextBlock Text="Modify Password" FontSize="18" FontWeight="SemiBold"/>
            <StackPanel Orientation="Horizontal" Spacing="10">
                <PasswordBox Width="250" PlaceholderText="New password"
                             Password="{x:Bind ViewModel.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Update" Width="100" Command="{x:Bind ViewModel.UpdatePasswordCommand}"/>
            </StackPanel>

            <TextBlock Text="Current Password (Required)" FontSize="18" FontWeight="SemiBold"/>
            <PasswordBox Width="250" PlaceholderText="Current password"
                         Password="{x:Bind ViewModel.CurrentPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" Foreground="Red" Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Spacing="20">
                <Button Content="Delete Account" Width="200" Height="40" Foreground="Red"
                        Command="{x:Bind ViewModel.DeleteAccountCommand}"/>
                <Button Content="Log out" Width="200" Height="40"
                        Command="{x:Bind ViewModel.LogoutCommand}"/>
                <Button Content="Go Back" Width="200" Height="40"
                        Command="{x:Bind ViewModel.GoBackCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>